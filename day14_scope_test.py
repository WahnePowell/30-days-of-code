#def computeDifference(self):
#        sorted_elements = sorted(self.__elements)
#        self.maximumDifference = abs(sorted_elements[-1] - sorted_elements[0])
 

#def computeDifference(self):
#    self.maximumDifference = abs(max(self.__elements) - min(self.__elements)) 

#def computeDifference(self):
#   	self.maximumDifference=max(a)-min(a)

#def computeDifference(self):
#    self.maximumDifference = max([abs(x-y) for x in self.numbers for y in self.numbers])

#def computeDifference(self):
#        self.maximumDifference = max(a) - min(a)

#    def computeDifference(self):
#        self.maximumDifference = None
#        for i, b in enumerate(self.__elements):
#            for c, k in enumerate(self.__elements):
#                if i != c:
#                    result = abs(self.__elements[c] - self.__elements[i])
#                    if self.maximumDifference is None:
#                        self.maximumDifference = result
#                    else:
#                        if self.maximumDifference < result:
#                            self.maximumDifference = result

#def computeDifference(self):
#        maxElement=max(self.__elements)
#        minElement=min(self.__elements)
#        self.maximumDifference=abs(maxElement - minElement)

#def computeDifference(self):
#        self.maximumDifference = max(self.__elements) - min(self.__elements)






